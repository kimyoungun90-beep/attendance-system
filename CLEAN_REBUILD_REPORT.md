# Clean v1 재구축 보고

- 기존 템플릿 기반 `final-template.js`를 사용하지 않고 새 `attendance-engine.js`를 작성했습니다.
- `year is not defined` 재발 방지를 위해 모든 월 계산을 `parseTargetMonth()` 결과로만 처리합니다.
- 휴무 자동 배정 로직은 넣지 않았습니다. 계획 공백을 앞에서부터 임의로 휴무 처리하지 않습니다.
- `출근증빙·휴무확인` 처리 완료값은 최종 상태로 인정하여 반차 확정 후 빨간색이 남지 않게 했습니다.
- `휴무 초과자` 시트는 기본 휴무 초과 인원만 표시합니다.
- `대체ㆍ보상` 시트는 전 인원 기준이며, 좌측에 당월 휴무 초과 여부를 표시합니다.
- `상담사근태`와 `상담사근태_관리자반영`은 A~M열과 1~6행 고정을 기준으로 생성합니다.
