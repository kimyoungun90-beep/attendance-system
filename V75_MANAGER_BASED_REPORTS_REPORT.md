# v75 관리자반영 기준 정산 시트 개편

- 엑셀 생성 버전을 v75로 변경했습니다.
- 임의 순서로 공백/미입력 날짜를 `휴무(공백)` 자동 배정하지 않도록 엑셀 생성 단계 호출을 중지했습니다.
- `출근증빙·휴무확인`은 기존 처리 방식은 유지하되, 자동 휴무 배정으로 숨겨지는 건이 없도록 조정했습니다.
- `휴무 초과자`는 `상담사근태_관리자반영` 시트의 날짜 셀 기준으로 재집계하며, 홈플러스는 월 6회, 전자랜드는 대상월 토·일 개수를 기준으로 초과자를 표시합니다.
- `대체ㆍ보상` 시트를 새로 만들고 전 인원 기준으로 당월 휴무 초과, 대체ㆍ보상 사용 가능 개수, 당월 사용 개수, 이월 개수, 당월 발생 개수, 초과 여부를 표시합니다.
- `연차 사용자` 시트를 새로 만들고 연차 신청 양식의 승인/반려/취소/철회 상태 기준으로 인정 여부를 표시합니다.
- 근무계획 또는 관리자반영에는 연차/반차이나 연차 신청 양식에 없는 건은 `계획 연차·신청 없음`으로 별도 표시합니다.
- `연차 촉진` 시트를 별도 생성해 기존 연차 누적 관리의 촉진 대상 목록을 분리했습니다.
- 근무계획, 근태 RAW, 중간저장/D1 관련 구조는 변경하지 않았습니다.
