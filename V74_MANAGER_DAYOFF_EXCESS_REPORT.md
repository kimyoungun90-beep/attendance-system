# v74 manager-reflected dayoff excess sheet

- v73 엑셀 생성 오류 수정본을 기준으로 이어서 수정했습니다.
- `휴무 초과자` 시트를 `상담사근태_관리자반영` 시트의 날짜 셀 기준으로 재계산하도록 변경했습니다.
- 매니저 월마감 수정본 반영 후 확정된 휴무/대체휴무/보상휴가 값으로 휴무 초과 여부를 다시 판단합니다.
- `휴무 초과자` 시트 순서를 5번째로 이동했습니다.
- `상담사근태_관리자반영` 시트도 `상담사근태`와 동일하게 1~6행과 A~M열을 고정합니다.
- D1, API, migrations, 인력 매칭, 연차 누적 관리 구조는 변경하지 않았습니다.
